# Node modules
node_modules/

# Build artifacts
dist/
src/generated/
dist/package.json
/dist/package.json

# Environment variables
.env
.env.local
.env.production
.env.*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# MacOS files
.DS_Store

# TypeScript cache
*.tsbuildinfo

# Prisma files
# prisma/migrations/
# prisma/*.db

# Docker
docker-compose.override.yml
docker-compose.yml

# VSCode settings
.vscode/

# Lock files
# package-lock.json is COMMITTED as of 2026-08-08 (supply-chain pin for the
# publish/CI chain — flagged by the post-audit security review). npm version
# keeps it synced during the CI bump.
yarn.lock

# Claude Code agent worktrees
.claude/

# Graphify code-graph output (regenerate with ../scripts/graphify-refresh.sh backend-legacy)
graphify-out/
